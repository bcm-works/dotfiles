# Custom 'config' directory
#   - Defaults to './config' (git ignored) if not set in a new file named '.env'
#   - The 'bin/setup.sh' script will add a symlink from this dir to './config'
export DOTFILES_CONFIG_DIR=/home/jane-doe/Synced/UserConfig

export DOTFILES_USER_NAME="Jane Doe"
export DOTFILES_USER_EMAIL="jane.doe@example.com"
