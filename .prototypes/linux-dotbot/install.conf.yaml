- defaults:
    link:
      create: true
      relink: true

# - clean: ['~']

- link:
    ~/.tmux.conf: tmux.conf
    ~/.vim: vim
    ~/.vimrc: vimrc
    # ~/Code: ~/Git

- create:
    -
    - ~/.vim/undo-history

- shell:
  - bash ~/Dotfiles/bin/setup.sh
  - bash ~/Dotfiles/fonts/fonts.sh
